<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AssinaturaDigital.Views.DocumentsPage">
    <ContentPage.Content>
        <StackLayout>
            <Label Style="{StaticResource title}"
                   Text="{Binding Title}" />
            <StackLayout Margin="10" VerticalOptions="CenterAndExpand">
                <Button Text="ENVIAR FOTO DO RG"
                        Command="{Binding RGCommand}" />
                <Button Text="ENVIAR FOTO DA CNH"
                        Command="{Binding CNHCommand}" />
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
